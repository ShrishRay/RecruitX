const { createModel } = require('../utils/memdb');

/**
 * Application Model — Powered by MemDB.
 */
const Application = createModel('Application', {
  // Simple schema for MemDB
});

module.exports = Application;
